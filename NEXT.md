# 다음 작업 — 인수인계(2026-09-27 작성)

이 파일은 **작업을 중단한 시점의 상태와 다음에 할 일**을 적는다. 성능 수치·아키텍처
서술의 원본은 README.md이고, 작업 규약은 CLAUDE.md다. 여기에는 "지금 어디까지 왔고
다음에 무엇을 어떤 순서로 하는가"만 적는다.

---

## 0. 현재 상태 한 줄

배포는 정상이고(`main` = CI green, 미커밋 없음), **막힌 작업은 없다.** 다음 작업은
**시간이 지나야 답이 나오는 측정 하나**를 기다렸다가 그 결과에 따라 갈린다.

| 출력 | 모델 | 상태 |
|---|---|---|
| 기온 + 예측구간 | 기온 전용 신경망 | MAE 0.851°C |
| 강수 + 예측구간 | 강수 전용 GBM | 발생 F1 0.628 · 구간 MAE 2.099mm |
| 극한기상 3종 | 극한 전용 GBM(단조제약) | 폭염 0.837 · 한파 0.654 · 황사 0.179 |

---

## 1. 바로 다음 — 에어로졸 드리프트 판정 ★최우선

### 무엇이 걸려 있나

외부 에어로졸(상류 5지점 CAMS)을 넣으면 **황사 F1이 0.179 → 0.224(+25%)** 로 오른다.
배포 가능한 형태(결측 NaN·전체 검증셋)에서 잰 값이다. 이득의 거의 전부가 상류에서
온다(+0.162 vs 관측소 자신 +0.074) — 설계 가설과 방향이 맞는다.

**채택을 막는 것은 성능이 아니라 시간 정합성이다.** 학습은 아카이브의 예측시점 값을
쓰고 서빙은 그 시각에 조회 가능한 값을 쓰는데, 대기질 API에는 수치예보의
`previous_day1` 같은 리드타임 보존 필드가 없어 **둘이 같은지 잰 적이 없다.**
CLAUDE.md 4절("리드타임이 다른 값을 학습·서빙에 나눠 쓰면 분포가 어긋난다")에 걸린다.

### 지금 돌아가고 있는 것

CI(`refresh-data.yml`)가 **6시간마다 1회** 에어로졸 값을 조회 시각과 함께 떠서
`data` 브랜치의 `cache/aerosol_snapshots.json`에 쌓는다(최근 20회 = 약 5일치 보관).
주기 제한은 스크립트가 스스로 건다(6시간 버킷당 1회) — CI가 15분마다 도는 잡에
얹혀 있어 호출부에 맡기면 같은 시간대에 네 번 뜬다.

### 할 일

```bash
# 1) data 브랜치의 스냅숏을 로컬로 받는다
curl -s https://raw.githubusercontent.com/yssong01/climate-forecast/data/cache/aerosol_snapshots.json \
  -o cache/aerosol_snapshots.json

# 2) 드리프트 판정
sg docker -c "docker run --rm -v \$(pwd):/app tri-chef-train-gpu:latest collect_aerosol_archive.py --drift"
```

판정은 **간격 48시간 이상**일 때만 유효하다(CAMS는 하루 두 번 발표하므로 그보다 짧은
간격의 "0% 변화"는 아무것도 증명하지 않는다 — 스크립트가 이 조건을 직접 검사해
"아직 결론이 아니다"라고 밝힌다).

### 결과에 따른 분기

**(A) 값이 바뀌지 않는다 → 채택 절차로 간다.** 다만 **단순히 열을 더하는 일이 아니다.**
막히는 지점을 미리 적어 둔다:

1. `extreme_gbm.py`가 `eval_cache.load_features()`의 28개 Z축 특징만 받는다 —
   에어로졸 열을 붙이는 경로를 만들어야 한다(`aerosol_gbm_check.py`의 `attach()`가
   그 원형이다. 결측은 NaN으로 둔다).
2. **서빙 가드가 막는다.** `predict.load_extreme_gbm()`이
   `meta_num_features != ckpt["num_features"]`이면 예외를 낸다. 의도된 가드이므로
   지우지 말고, "극한 GBM은 주 체크포인트보다 열이 많을 수 있다"를 명시적으로
   표현하도록 고친다.
3. **서빙에 에어로졸 값이 없다.** NWP와 같은 패턴이 필요하다 — CI가
   `cache/aerosol_recent.json`을 만들어 `data` 브랜치에 올리고 앱이 그것만 읽는다
   (배포 앱이 직접 API를 호출하지 않는다는 규약, CLAUDE.md 4절).
4. 승격 게이트 3종은 입력 벡터를 각자 만든다 — CLAUDE.md **7-1항** 적용 대상이다.
5. 황사는 화면에 표시하지 않으므로, 채택해도 **화면 문구는 '성능 검증' 탭의 수치만**
   바뀐다. 그래도 `capture_tab_screenshots.py`는 돌린다.

**(B) 값이 바뀐다 → 그만큼을 뺀 값으로 다시 판단한다.** 차이가 곧 학습·서빙 격차다.
이 경우 "리드타임을 보존하는 자료원을 찾는다"가 다음 질문이 된다.

---

## 2. 그다음 — 온라인 적중률로 배포본을 검증

화면의 강수 적중률은 이제 **기저율과 F1을 함께** 표시한다(2026-09-27). 현재 배포
신원(`sha256:b9635a113`)에서는 아직 비가 온 표본이 0건이라 화면이 "아직 판별력을
재지 않는다"고 밝히고 있다.

**비가 오는 구간이 지나가면** 온라인 F1을 검증값(0.628)과 대조한다. 크게 어긋나면
서빙 경로(실시간 관측·NWP 창)의 문제를 의심한다 — 검증셋은 아카이브를 쓰고 서빙은
실시간 조회를 쓰므로 둘이 다를 수 있고, 그 차이를 이 지표만 볼 수 있다.

```bash
curl -s https://raw.githubusercontent.com/yssong01/climate-forecast/data/cache/accuracy_log.json | \
  python3 -c "import json,sys,collections; ..."   # 본문 3절의 분해 코드 참고
```

---

## 3. 새 실험을 시작한다면

- **기준선은 GBM이다.** 신경망 대비 개선은 의미가 없다(README '기준선 실측' 절).
  실험 채점: 기온 MAE 0.851 · 강수 발생 F1 0.6397 · 한파 0.6077.
- **재학습 25분을 쓰기 전에 `baseline_suite.py`에 열을 추가해 GBM으로 먼저 재라** —
  수십 배 싸고 같은 질문에 답한다.
- **두 채점을 섞지 말 것.** 실험 채점(평가용 절반, `honest_f1`)과 서빙 채점(공식 라벨
  전체)은 값이 다르다. 표를 넘나들며 인용하면 안 된다.

---

## 4. 닫힌 질문 — 다시 열지 말 것

| 질문 | 결론 | 근거 |
|---|---|---|
| 강수 전체 MAE를 공짜로 줄일 수 있나 | **없다.** 손잡이 두 개(τ·바닥값) 다 거래다 | `precip_floor_check.py` — 0.1mm 미만 행이 201/203,871개뿐 |
| 한파 단조성 | **해결.** 인접 쌍 위반 0/1,440 | `monotonic_cst`, 두 리드타임 |
| 3축 융합이 도움이 되나 | **아니다.** 산출물에서 빠졌다 | 게이트가 Z에 1.000, `zonly`가 동일 성능 |
| 진단 스크립트 동시 실행 | **가능하다** | 5개 eval_cache 전환, RAM 2GiB 확인 |

---

## 5. 환경 확인(재개 시 한 번)

```bash
cd /mnt/c/yssong/climate-forecast
git fetch origin && git status --short --branch     # 미커밋 0 · origin/main과 동일해야 한다
sg docker -c "docker run --rm -v \$(pwd):/app tri-chef-train-gpu:latest test_diagnostics_smoke.py"
free -g                                             # 학습은 순차로만(RAM 21.9/31.2GiB)
```
