# 환경 예보 (climate-forecast)

기상청 ASOS 지상관측 데이터로 **+6시간 뒤 기온·강수·극한기상 확률**을 산출하는 3축 멀티모달 파이프라인입니다.

논문 **Tri-CHEF**(*Complex-Hermitian Embedding Fusion for Korean Multimodal Retrieval*, Zenodo 프리프린트, DOI [10.5281/zenodo.20034370](https://doi.org/10.5281/zenodo.20034370))의 **3축 융합 수식만** 빌려와, 원 논문의 도메인(한국어 멀티모달 **검색**)과 전혀 다른 기후 예측에 이식한 실험입니다. 그 과정에서 발견한 문제들을 수식·코드·ablation으로 추적한 기록이기도 합니다.

> ⚠️ 이 저장소의 산출물은 **모델 출력값**이며 기상청의 공식 예보·특보가 아닙니다.
> 기상산업진흥법상 예보업은 등록 대상이므로, 실제 방재 판단의 근거로 사용해서는 안 됩니다.

### 지금 성능은? (한눈 요약, 그룹 분할 검증 기준·2026-08-16)

| 항목 | 값 | 판정 |
|---|---|---|
| 기온 MAE | 1.30°C | 퍼시스턴스 대비 **+61.2%** |
| 강수 MAE | 0.178mm | 상시0 대비 **17.8% 미달** (아직 못 넘김) |
| 폭염 F1 / 한파 F1 | 0.808 / 0.490 | — |

자세한 근거·조건·한계는 [결과 요약](#결과-요약)에 있습니다.

## 실행 중인 대시보드

▶ **[climate-forecast.streamlit.app](https://climate-forecast-bpvbmtrt3zj4l8fjtcipad.streamlit.app/)**

12개 관측소를 선택해 실시간 관측값과 +6시간 뒤 산출값, 극한기상 확률, 성능 지표,
모델 내부 구조를 확인할 수 있습니다.

> **접속 시 참고 — 앱이 절전 상태일 수 있습니다.**
> Streamlit Community Cloud는 일정 시간 접속이 없으면 앱을 중지시킵니다.
> "This app has gone to sleep" 화면이 나오면 깨우기 버튼을 누르고 1~2분 기다려 주세요.
> 아래 스크린샷으로 주요 화면을 먼저 확인하실 수 있습니다.

### 출력값 추이

선과 막대는 최근 72시간의 **실측값**이고, ★ 표시가 이번 +6시간 산출값입니다.
예측 대상은 기온·강수 두 가지이며, 습도·기압은 상황 판단을 돕기 위한 참고용 실측값입니다.

![출력값 추이 탭](docs/images/tab-trend.png)

### 극한 기상

폭염·한파·황사 발생 확률과 판정 임계값(빨간 점선)을 함께 표시합니다.
각 확률이 어떤 정답 라벨로 학습되었는지, 그 라벨의 한계는 무엇인지도 화면에 명시합니다.

![극한 기상 탭](docs/images/tab-extreme.png)

### 성능 검증

회귀 MAE를 **기준선(학습을 전혀 하지 않는 자명한 방법) 대비 증감과 함께** 표시합니다.
MAE는 그 자체로는 좋고 나쁨을 말해주지 않고, 기준선보다 나은지로 판단해야 의미가 생깁니다.

- 기온 1.30°C — 퍼시스턴스(T(t+6)≈T(t)) 대비 **+61.2% 정확**
- 강수 0.178mm — '상시 0mm' 대비 **17.8% 미달**. 넘지 못한 지표도 그대로 노출합니다

> 2026-08-16 검증 방식을 바꾸며 위 수치가 이전 버전(기온 1.26°C·강수 0.9%
> 미달)보다 나빠 보이는데, 모델이 나빠진 게 아니라 검증 방식의 결함을
> 고치며 부풀림이 걷힌 것입니다. 자세한 경위는 [정직한 한계 9번](#9-검증-분할이-일-단위-라벨을-누수시키고-있었습니다)을 참고하세요.
> 같은 날 안에서 입력 특징을 14차원(연중 시각 sin/cos 추가)으로 늘려 재학습해
> 기온이 1.3551°C→1.2987°C 로 다시 개선됐습니다 — 아래 "연중 주기 특징
> 추가(A안)" 참고.

극한기상은 정확도 대신 정밀도·재현율·F1로 평가합니다. 폭염이 전체의 1%뿐이라면
'항상 폭염 아님'이라고만 답해도 정확도가 99%가 되기 때문입니다.

![성능 검증 탭](docs/images/tab-performance.png)

### 모델 구조

출력마다 재계산되는 축별 기여도, 3축의 정의, 융합 수식의 기호별 해설,
원 논문과의 관계를 화면에서 확인할 수 있습니다.

![모델 구조 탭](docs/images/tab-model.png)

## API 사용 시 주의사항

이 저장소를 직접 실행하려면 기상청 API허브(apihub.kma.go.kr) 인증키가 필요합니다.
**아래 제약을 먼저 확인하시기 바랍니다.**

### 1. 호출량 — 산출값 1건당 15회

| 구성 | 호출 수 |
|---|---|
| 대상 관측소 현재 관측 | 1 |
| 인접 11개 관측소 (Re축 공간 보간장) | 11 |
| 대상 관측소의 1·3·6시간 전 관측 (Im축 경향 벡터) | 3 |
| **합계** | **15** |

이 비용은 **접속자 수 × 화면 갱신 횟수**로 곱해집니다. 대시보드 기본 갱신 주기가
5분이므로, 캐시가 없으면 1개 관측소만 열어두어도 하루 4,320회에 도달합니다.

### 2. 차단 이력 — 누적 약 9,800건

대량 수집을 시도했을 때 **누적 약 9,800건 시점에서 차단**되었습니다
(2026-08-07 측정, 동시성 16 조건). 병렬화로 우회되지 않는 요청 수 기준의
제한으로 판단됩니다. 따라서 **과거 데이터 백필을 API로 시도하지 마십시오** —
기상자료개방포털의 파일셋(CSV 일괄 다운로드)을 사용해야 합니다.

### 3. 이 저장소의 대응

| 장치 | 내용 |
|---|---|
| **관측 시각 단위 캐시** | 산출 결과를 `(관측소, 관측 시각)` 키로 캐시합니다. ASOS는 매시 정각 1회 관측이므로 같은 정시 내 재조회는 동일한 값을 반환합니다. 따라서 호출량이 갱신 주기·접속자 수와 무관해지고 **관측소당 시간당 15회**로 고정됩니다 |
| **일간 상한** | `KMA_DAILY_CALL_BUDGET`(기본 3,000). 초과 시 예외를 발생시키지 않고 폴백으로 전환하며, 화면 상단에 그 상태를 표시합니다 |
| **사용량 표시** | 사이드바에 당일 누적 호출 수를 실측으로 노출합니다 |
| **폴백 데이터 격리** | `SUCCESS_LIVE` 응답만 모델 입력에 사용합니다. 폴백 레코드를 사용하면 합성 기본값이 실측으로 간주되어 보간장에 반영되기 때문입니다 |

검증 결과: 동일 정시에 2회 실행 시 호출 증가 **0건**.
실사용 기준 합산 예산은 대시보드 360~720회에 자동 갱신 288회를 더해
**약 650~1,000회/일**이며, 차단 지점의 7~10% 수준입니다.

### 4. 키 관리

인증키는 용도별로 분리해 두며, 저장소에는 포함되지 않습니다.

| 실행 환경 | 저장 위치 |
|---|---|
| 로컬 실행 | `.env` (gitignore 대상) |
| Streamlit Cloud 배포 | 앱 설정의 Secrets |
| GitHub Actions 자동 갱신 | 저장소 Secrets |

```toml
# Streamlit Secrets 형식 — 값은 반드시 큰따옴표로 감쌉니다
KMA_API_KEY = "발급받은_키"
KMA_STN = "108"
```

> 따옴표를 생략하면 TOML 파싱이 실패하여 앱이 키를 읽지 못합니다.
> 이 경우 화면은 대체값으로 정상 표시되므로 식별이 어렵습니다 —
> 사이드바의 API 호출 수가 0이면 이 상황에 해당합니다.

## 결과 요약

**학습 데이터**: 2013-01-01 ~ 2026-08-10(약 13.6년), 12개 관측소(서울·인천·수원·춘천·강릉·대전·청주·전주·광주·대구·부산·제주) 통합, ASOS 시간자료 **1,382,583건** → (t, t+6h) 유효 쌍 **1,380,966개**.

### 검증 방식을 다시 세웠습니다 (2026-08-16)

**학습·검증 분할을 시각 단위 무작위에서 날짜 단위 그룹 분할로 바꿨습니다.**
폭염·한파·황사 라벨은 (관측소, 날짜) 단위인데 — 특보가 발효된 날은 그 날
24개 시각 표본이 전부 같은 라벨을 받습니다 — 분할은 시각 단위 무작위였습니다.
실측 결과 판정 가능한 검증 표본의 **100.00%가 학습셋에 같은 날짜 짝을
가지고 있었습니다**(양성 표본만 따져도 100.00%). 모델이 그 관측소·그
날짜의 정답을 학습 중에 이미 본 상태에서 채점받고 있었다는 뜻입니다.
날짜를 통째로 한쪽에 배정하는 그룹 분할로 바꾸자 극한기상 F1이 크게
내려갔습니다 — **모델이 나빠진 게 아니라 부풀림이 걷힌 것**입니다.
자세한 경위와 실측 수치는 [정직한 한계 9번](#9-검증-분할이-일-단위-라벨을-누수시키고-있었습니다)에 있습니다.

아래 결과는 전부 이 새 방식(그룹 분할)으로 잰 것입니다. 아래에 남겨둔
v3~v7·이전 3축 적용 효과 표는 무작위 분할로 측정한 **이전 세대 기록**이며,
비교 목적이 아니라 이력 보존을 위해 그대로 둡니다.

### 13년 데이터 재학습 결과 (그룹 분할 검증, 표본 276,116개)

| 지표 | 모델 | 기준선 (학습 없는 자명한 방법) | 판정 |
|---|---|---|---|
| 기온 MAE | **1.2987 °C** | 퍼시스턴스 T(t+6)≈T(t) : 3.3452 °C | **−61.2%** (개선) |
| 강수 MAE | 0.1784 mm | 상시 0mm 예측 : 0.1515 mm | **17.8% 미달** |

강수는 이전(무작위 분할 기준 0.8% 미달)보다 격차가 크게 벌어져 보이지만,
이 역시 검증 방식을 바꾼 효과가 섞여 있습니다 — 인접 시각 표본 간 자기상관도
무작위 분할에서는 일부 새어 들어가고 있었습니다. 헤드 4개(강수·폭염·한파·
황사)가 공유 표현을 두고 경쟁하는 구조적 원인은 그대로 남아 있어, 이 지표는
여전히 개선 과제입니다.

동적 게이팅이 학습한 축 배분: **공간보간장(Re) 44.6% · 시간경향벡터(Im) 18.1% · 수치 센서(Z) 37.3%**
(게이트 엔트로피 1.0355 / 최대 1.0986)

### 연중 주기 특징 추가 (A안, 2026-08-16)

`partition_heterogeneity.py`로 분할 후보 축을 실측한 결과, 관측소(η²=0.00%)는
공간 분할 근거가 없었지만 **월(계절)은 입력 어디에도 없는 채로 빠져 있었고**,
시각을 이미 아는 상태에서도 월을 추가로 알면 기온 변화(ΔT) 설명력이
+2.59%p(약 6.2배) 더 올라갔습니다. 이 근거로 수치 입력에 연중 시각
sin/cos(day-of-year, 12→14차원)를 추가해 재학습했습니다 — 같은 그룹 분할
검증셋, 같은 학습 설정(배치 1024·LR 1e-3) 기준입니다.

| 지표 | 이전(12차원) | A안(14차원) | 변화 |
|---|---|---|---|
| 기온 MAE | 1.3551 °C | 1.2987 °C | **−4.2%** (개선) |
| 강수 MAE | 0.1786 mm | 0.1784 mm | 거의 무변화 |
| 폭염 F1 | 0.813 | 0.808 | 거의 무변화(오차 범위로 판단) |
| 한파 F1 | 0.384(디커플링 전 기준) | 0.397 | 소폭 개선 |
| 황사 F1 | 0.150 | 0.166 | 소폭 개선 |

기온이 가장 뚜렷하게 개선됐고, 이는 위 η² 실측과 방향이 일치합니다.
나머지는 소폭 변화라 통계적 유의성 검정 없이 "확실한 개선"이라고까지는
말할 수 없습니다.

### 극한기상 분류 성능 (그룹 분할 검증, 판정 임계값 0.5 기준)

| 사건 | 정밀도 | 재현율 | F1 | 검증셋 양성 |
|---|---|---|---|---|
| 폭염 | 74.4% | 88.4% | 0.808 | 15,525건 |
| 한파 | 48.9% | 49.1% | 0.490 | 3,864건 |

위 한파 수치는 A안 재학습 후 한파 헤드만 다시 디커플링(트렁크 동결·헤드만
미세조정, Kang et al. 2020 방식)한 결과입니다. 디커플링 직후(A안 기준
0.397)에서 F1 0.490까지 올랐고, 기온·강수·폭염·황사는 완전히 그대로임을
확인했습니다(트렁크 동결이 다른 헤드에 영향을 주지 않는지 검증).

임계값을 검증셋 절반(보정용)에서 재보정하고 나머지 절반(평가용)에서 확인한
결과, **강수만 재보정값(t=0.83, 순이득 +0.101)을 채택**했습니다. 폭염·한파는
순이득이 각각 +0.007·+0.001로 유의하지 않아 기본값 0.5를 그대로 씁니다.
**한파는 이전 체크포인트(디커플링 전 아키텍처)에서는 재보정(t=0.89)이
유의미했는데(순이득 +0.095), 이번엔 근거가 사라졌습니다** — 헤드를 다시
미세조정하면서 확률 분포 자체가 바뀌었기 때문입니다. 임계값은 헤드가
바뀔 때마다 그 헤드 기준으로 다시 골라야 한다는 뜻입니다.

**황사는 이 표와 대시보드에서 뺐습니다.** 재보정(t=0.86, 순이득 +0.021은
채택)해도 정밀도 13.4%에 그쳐 — 황사라고 판정한 것 중 86.6%가 오탐입니다.
PM10 농도는 국외 장거리 수송이 지배적인데 이 모델의 입력은 국내 12개
지점의 기상 관측뿐이라, 구조적으로 판별 신호가 부족한 것으로 보입니다.
헤드 자체는 남아 있고 계속 계산되지만 화면에는 내놓지 않습니다.

이전 세대(3.6년 데이터·무작위 분할) 축 구성별 비교, Phase 3-6 경보 게이트 지표는
[부록 — 이전 세대 기록](#부록--이전-세대-기록)으로 옮겼습니다.

## 데이터 출처

**기상청 기상자료개방포털** (https://data.kma.go.kr/) — '파일셋' 메뉴에서 CSV 일괄 다운로드(시간자료, 2013~2026).

| 포털 내 경로 | 사용처 |
|---|---|
| 데이터 → 기상관측 → 지상 → **종관기상관측(ASOS)** | Z·Re·Im 3축의 원천 관측값 (1,382,583건, 12개 관측소 × 2013~2026) |
| 데이터 → 기상관측 → 지상 → **황사관측(PM10)** | 황사 헤드 정답 라벨(PM10 1시간 평균 150㎍/㎥ 기준). 12개 중 6곳만 관측망 보유 — 현재 헤드는 계산되지만 신뢰도 부족으로 화면엔 표시하지 않는다(위 '극한기상 분류 성능' 참고) |
| **날씨 이슈별 데이터** → 폭염 / 한파 / 황사 / 태풍 | 폭염·한파 헤드 정답 라벨 (실제 발표된 특보 기록만 사용 — 기록이 없는 표본은 학습·채점에서 제외). 포털 제공 기간이 폭염·황사는 2019년~, 한파는 2020년~로 못박혀 있다(2026-08-16 직접 확인) — 다운로드 누락이 아니라 포털 자체의 한계라 표본을 더 늘릴 수 없다. 태풍은 폴더만 받아뒀고 아직 어떤 헤드도 쓰지 않는다 |

**API가 아니라 웹 포털 CSV로 백필한 이유**: apihub API로 대량 수집을 시도했을 때 **누적 약 9,800건 지점에서 차단**됐습니다(2026-08-07 실측, 동시성 16). 병렬화로 우회되지 않는 요청수 기준 상한이라, 과거 데이터 백필은 포털 CSV로 전환하고 API는 실시간 추론에만 씁니다(`import_kma_fileset.py`).

> 원본 CSV는 **EUC-KR** 인코딩입니다. UTF-8로 읽거나 grep하면 조용히 실패합니다.

## 아키텍처

```
                        ┌──────────────┐
  이웃 11개 관측소 ──▶     Re 인코더    │───┐
  IDW 공간보간장           (소형 CNN)       │          
  (4ch, 32×32)          └──────────────┘   │     ModalityGate
                        ┌──────────────┐   │     (입력 조건부         s_i = √( (w_Re·Re_i)²
  1·3·6h 전 대비   ──▶     Im 인코더   │─── │──▶ softmax 배분) ──▶        + (w_Im·Im_i)²
  변화량 (12d)             (프로젝션)        │         Σw = 1               + (w_Z ·Z_i )² )
                        └──────────────┘   │                                 │
                        ┌──────────────┐   │                                 ▼
  ASOS 수치 (14d)  ──▶     Z 인코더    │───┘                    ┌─────────────────────────┐
                             (MLP)                                회귀 2 : 기온(Δ) · 강수     
                        └──────────────┘                          분류 3 : 폭염·한파·황사     
                                                                └─────────────────────────┘
```

세 축은 각각 **다른 질문**에 답하고, 그 답을 하나로 합칩니다.

| 축 | 던지는 질문 | 입력 | 왜 Z축에서 유도 불가능한가 |
|---|---|---|---|
| **Z** (지금 여기) | 이 지점은 지금 어떤 상태인가? | 기온·강수·습도·풍속·풍향(sin/cos)·기압·강수형태·시각(sin/cos)·위도·경도·연중 시각(sin/cos) = 14차원(2026-08-16 연중 주기 추가) | — (기준축) |
| **Re** (주변) | 주변 지역은 지금 어떤가? | 대상 관측소를 **제외한** 11개 관측소의 같은 시각 실측을 IDW(역거리가중, power=2)로 ±3°(≈300km) 격자 32×32에 보간, 4채널(기온·습도·기압·풍속) | 다른 지점의 관측값은 이 지점 스냅샷에 존재하지 않음 |
| **Im** (흐름) | 어느 쪽으로 움직이는 중인가? | 같은 관측소의 1·3·6시간 전 대비 변화량 × 4변수 = 12차원 | ∂s/∂t 는 단일 시점 s(t) 하나만으로 계산 불가 |

- **융합**: 논문 Eq.1의 Hermitian-style modulus를 **원소별**로 적용 → 길이 64 벡터 `s`
- **축 가중치**: 고정 상수가 아니라 `ModalityGate`(14→16→3, softmax)가 **매 출력마다 재계산**. Σw=1로 고정해 스케일 자유도를 제거한다
- **회귀 헤드**: 기온은 퍼시스턴스 잔차(Δ) 방식, 강수는 Hurdle 구조 `σ(z₁) × softplus(z₂)`
- **분류 헤드**: 폭염·한파·황사 3종, 각각 독립 BCE. 대설은 한파∧호우의 논리곱이라 중복계산이 되므로 두지 않았다

## 실행

```bash
pip install -r requirements.txt      # 아래 "환경 주의사항" 먼저 읽어주세요
cp .env.example .env                 # KMA_API_KEY 발급 후 입력 (apihub.kma.go.kr)

python test_phase34.py               # 수치 안정성 19개 항목 검증
python test_phase36.py               # 경보 게이트 36개 항목 검증
python train.py                      # 12개 관측소 수집(~40분) + 학습
python predict.py --stn 108          # 단발 추론 (CLI)
python rl_phase_filter.py            # Phase 3-6 경보 게이트 4-fold 교차검증
streamlit run app.py                 # 실시간 대시보드
```

`predict.py --list-stations`로 관측소 코드 목록 확인 가능.

### Docker로 대시보드만 실행

학습은 로컬 GPU 환경에서 미리 마쳤다고 가정한다 — 이미지에는 체크포인트를
포함하지 않고 런타임에 볼륨으로 마운트한다(`Dockerfile` 상단 주석 참고).

```bash
docker build -t tri-chef-app .
docker run -p 8501:8501 \
  -v "$(pwd)/checkpoints:/app/checkpoints" \
  -v "$(pwd)/cache:/app/cache" \
  --env-file .env \
  tri-chef-app
```

**코드 수정이 바로 반영되게 실행** — 위 명령은 이미지에 복사된 소스를 쓰므로,
`.py`를 고쳐도 실행 중인 컨테이너 화면은 그대로다(재빌드해야 반영된다). 화면
문구나 로직을 고치며 확인할 때는 소스도 함께 마운트한다.

```bash
docker run --rm -p 8501:8501 \
  -v "$(pwd):/app" \
  --env-file .env \
  tri-chef-app
```

이렇게 띄우면 파일을 저장할 때마다 Streamlit 우측 상단에 "Source file changed"가
뜨고 **Rerun**을 누르면 즉시 반영된다(항상 자동 적용하려면 ⋮ → Settings →
Run on save).

CI(`.github/workflows/ci.yml`)는 push/PR마다 수치 안정성 19개 항목
(`test_phase34.py`), 경보 게이트 36개 항목(`test_phase36.py`), Dockerfile
빌드를 자동 검증한다.

### Streamlit Community Cloud 배포

저장소 루트의 `requirements.txt` 하나만 보고 빌드된다. Main file path는
`app.py`, Python 버전은 3.12(학습 컨테이너와 동일). Secrets에 아래를 넣는다.

```toml
KMA_API_KEY = "발급받은_키"
KMA_STN = "108"
```

배포에 필요한 데이터는 저장소에 커밋되어 있다 — 체크포인트(233KB), 최근
관측 창(0.6MB), 적중률 로그(12KB, 2026-08-16 백테스트 시딩분 분리 후).
학습용 원본(`text_embeddings.npz` 460MB, `historical_data_1y.json` 69MB)은
`.gitignore`가 계속 막는다.

### API 트래픽 — 5분 갱신이 왜 부하가 아닌가

**출력값 1건 = API 15회.** 대상 관측소 1 + 이웃 11곳(Re축 공간보간장) + 대상
관측소의 과거 1·3·6시간(Im축 경향벡터) = 15회.

문제는 이 비용이 **접속자 수 × 갱신 횟수**로 곱해진다는 점이다. 이 키는 누적
약 9,800건에서 차단된 이력이 있어(2026-08-07 실측) 그대로 두면 이틀이면
막힌다. 대시보드 기본 갱신 주기가 5분이므로:

| 조건 | 시간당 호출 | 일일 호출 |
|---|---|---|
| 캐시 없음, 5분 갱신, 1개 관측소 | 12회 × 15 = **180** | **4,320** |
| 캐시 없음, 5분 갱신, 접속자 3명 | 540 | 12,960 (**차단 지점 초과**) |
| **캐시 적용** (관측소·관측시각 단위) | **15** | **360** |

핵심은 **ASOS가 매시 정각 관측 1회, 약 10분 후 공개**라는 물리적 사실이다.
같은 정시 안에서 몇 번을 다시 조회해도 서버는 같은 값을 돌려준다. 그래서
`app.py`가 예측 결과를 `(관측소, 관측시각)` 키로 캐시하면:

```python
@st.cache_data(ttl=3600, show_spinner="관측 조회 중... (12개 관측소)")
def cached_predict(stn: str, obs_hour: str, _model, _ckpt) -> dict:
    return predict(stn=stn, model=_model, ckpt=_ckpt)
```

- 호출량이 **갱신 주기·접속자 수와 무관해지고 시계에만 묶인다**
- 관측소당 시간당 15회가 절대 상한 → 12개 관측소를 매시간 전부 조회해도 최악 4,320회/일
- 실사용(1~2개 관측소)에서는 **360~720회/일**, 차단 지점의 **3.7~7.3%**

실측 검증: AppTest로 같은 정시에 두 번 실행 → `1회차 15건 → 2회차 누적 15건
(증가 0건)`.

여기에 2차 안전장치로 프로세스 단위 일일 상한(`KMA_DAILY_CALL_BUDGET`, 기본
3,000)을 둔다. 상한 초과 시 예외 대신 폴백으로 내려가고, 사용량은 사이드바에
실측으로 표시된다. 상한 5건으로 강제한 실험에서 `호출 5건 / 차단 10건`으로
정상 동작을 확인했다.

**합산 일일 예산**: 대시보드 360~720회 + GitHub Actions 데이터 갱신 288회
≈ **650~1,000회/일** — 차단 지점(9,800)의 **7~10%** 수준이다.

**데이터 갱신 — 저장소가 원본이다.** Streamlit Cloud의 컨테이너
파일시스템은 휘발성이라 앱이 쓴 파일은 재시작 시 사라진다. 그래서 갱신
주체를 앱이 아니라 저장소로 둔다:
`.github/workflows/refresh-data.yml`이 6시간마다
`refresh_deploy_data.py`를 돌려 ① 최근 관측 창을 채우고 ② 적중률 로그의
대기 항목을 실측과 대조한 뒤, 결과를 main에 되커밋한다. Streamlit Cloud가
그 푸시를 감지해 자동 재배포하면서 새 데이터가 반영된다. 이 워크플로에도
`KMA_API_KEY` 시크릿이 필요하다(회당 약 72건, 하루 약 288건).

**공개 범위.** Community Cloud 앱은 기본이 공개 URL이다. 기상산업진흥법상
예보업 등록 대상이 아닌 범위로 유지하려면 앱 설정에서 뷰어 허용목록
(Private)으로 두는 것을 권한다 — 자세한 내용은 아래 "법적 범위" 항목 참고.

## 정직한 한계

본 프로젝트는 성능 달성보다 **설계 결함을 실험으로 규명하고 그 근거를 제시하는 과정**에 중점을 두었습니다. 아래 9개 항목은 현 시점에서 확인된 한계와 그 원인 분석입니다(항목을 눌러서 펼치세요).

<details>
<summary><b>1. 시뮬레이션 모달리티는 정보량이 정확히 0이었습니다 (→ 실측으로 교체 완료)</b></summary>

초기 Re축(합성 위성)과 Im축(MiniLM 텍스트)은 **Z축 수치에서 결정론적으로 생성한 시뮬레이션**이었습니다. 정보이론적으로 Z를 넘을 수 없는 구조인데, 실측이 이를 그대로 확인했습니다.

- **결정적 증거**: `deploy_ablation.py`에서 추론 시 Re축을 영벡터로 꺼도 출력이 **소수점까지 완전히 동일**했습니다(기여도 정확히 0).
- **게이트의 판단도 일치**: 게이트가 Re축에 배분한 가중치가 v4 시점에 **0.000**이었습니다. 모델이 스스로 "볼 가치가 없다"고 학습한 것입니다.

이후 **"단일 시점 스냅샷(Z)에서 유도할 수 없는 정보만 새 축에 담는다"**는 기준으로 재설계했습니다.

| 교체 | 이전 | 이후 | 결과 |
|---|---|---|---|
| Re축 | 합성 위성 이미지 | 11개 관측소 실측 IDW 공간보간장 | 게이트 배분 0.00 → **0.52**, 황사 F1 0.257 → **0.661** |
| Im축 | MiniLM 시뮬레이션 예보문 | 1·3·6h 전 대비 실측 변화량 | 게이트 배분 0.12 → 0.21 (F1은 일부 하락, 아래 참조) |

**Im축 교체는 성능이 오히려 떨어졌지만 그대로 유지했습니다.** MiniLM 대비 4개 F1 지표가 전부 낮습니다(강수 0.585→0.571, 폭염 0.880→0.862, 한파 0.765→0.730, 황사 0.671→0.602). 인코더 용량을 32→128로 키워도 회복되지 않아 "용량 부족" 가설은 기각됐습니다. 그럼에도 **"가짜 데이터를 입력으로 쓰지 않는다"**는 원칙을 성능보다 우선해 실측 유지를 택했고, 손해를 숨기지 않고 명시합니다. MiniLM은 실측 Z축의 재포장이라 완전한 가짜(합성 위성)와는 성격이 다르지만, 원칙의 일관성을 택했습니다.

부수 효과로 `sentence-transformers`(약 450MB) 의존성이 서빙 경로에서 완전히 사라져 배포 컨테이너 메모리가 **227.9MiB**(docker stats 실측)로 줄었습니다.

</details>

<details>
<summary><b>2. Gram-Schmidt 직교화는 이 도메인에서 손해였습니다</b></summary>

논문은 검색 태스크에서 축 독립성(직교성)을 전제하지만, 실측(`ablation.py`)에서 직교화를 강제하면 성능이 하락했습니다. 네트워크가 자유롭게 학습한 Re-Im 상관도(0.48)가 오히려 회귀 성능에 기여하고 있었습니다 — 축 간 정보 공유가 강건성으로 작용하는 회귀 태스크와, 독립성이 곧 변별력인 검색 태스크는 다릅니다. 기본값은 `ORTHOGONALIZE = False`로 되돌렸습니다.

</details>

<details>
<summary><b>3. 정적 축 가중치는 무용한 축을 억제하지 못했습니다</b></summary>

학습 가능한 α/φ(log-parameter)는 정보가 없는 위성 축의 가중치를 오히려 키웠습니다(α 0.4→0.665). 원인은 두 가지: (a) 논문 기본값 자체가 위성 축을 1순위로 두는 초기화였고, (b) ResNet18(1,100만 파라미터)이 학습 표본(572개)을 그대로 암기해 훈련 손실 기준으로는 "가장 잘 맞는 축"으로 보였기 때문입니다. `ModalityGate`(균등 초기화 + 엔트로피 정규화 워밍업) + 인코더 축소(2.8만 파라미터)로 이 문제를 해결했습니다.

</details>

<details>
<summary><b>4. 1시간 예보는 무의미했습니다 / 강수는 여전히 '상시 0mm' 기준선보다 나쁩니다</b></summary>

1시간 후 기온은 퍼시스턴스(T(t+1)≈T(t))가 사실상 무적이라 어떤 모델도 유의미한 개선을 보여줄 수 없었습니다(+0.7%). 시계를 6시간으로 늘리자 시각(일주기) 특성과 축 융합이 실제로 기여하기 시작했습니다. 이 판단으로 `lead_hours = 6`을 확정했습니다.

> 위 +0.7%는 초기 30일 수집본(검증셋 1,709개) 기준 측정치입니다. 현행 3.5년 데이터셋(검증셋 약 66,000개)에서는 재측정하지 않았으므로, 시계 선택의 **근거로만** 인용하고 현행 성능 수치로 쓰지 않습니다.

가장 크게 실패한 지점이고, 가리지 않고 화면에도 표시합니다. **다만 2026-08-11 재학습에서 격차를 +0.8%까지 좁혔습니다**(이전 −19.3%). 강수는 대부분의 시각이 0mm라 **아무 계산 없이 "항상 0mm"라고만 답해도** MAE가 매우 낮게 나옵니다. 현행 모델의 강수 MAE는 그 기준선을 **넘지 못했습니다**.

원인은 구조적으로 추적됐습니다 — 극한기상 헤드가 3개(폭염→한파→황사)로 늘어나면서, 헤드들이 **하나의 공유 magnitude 표현(`s`)을 두고 경쟁**해 Hurdle의 무강수 억제력이 약해졌습니다(baseline 대비 −16.0% → −25.6%). `EXTREME_BCE_WEIGHT`를 1.0 → 0.3으로 낮춰 −19.3%까지 완화했고, 추론 후처리로 0.2mm 미만을 0으로 반올림해 남은 손해를 줄였습니다. **새 헤드를 추가할 때마다 강수 MAE를 반드시 재확인**하는 것이 이 프로젝트의 규약이 된 이유입니다.

강수는 MAE보다 "비가 올지 안 올지 맞혔나"가 실용 가치에 가깝고, 그 지표는 대시보드 '성능 검증' 탭의 강수 적중률(0.1mm 기준 비/무비 판정 일치)로 따로 봅니다.

> 기준선 수치는 학습 시 계산은 하지만 체크포인트에 저장하지 않던 버전이라, 현행 체크포인트로는 화면에서 직접 비교하지 못합니다. `train.py`에 `val_temp_naive_mae`·`val_precip_naive_mae` 저장을 추가해 다음 재학습부터는 대시보드에 증감으로 자동 표시됩니다.

</details>

<details>
<summary><b>5. 이 프로젝트 자체의 디버깅 과정에서 잡은 실제 버그들</b></summary>

- `torch.clamp(x, 0, 200)`이 x<0에서 gradient가 정확히 0이 되어, 강수 예측 헤드가 전 샘플 음수로 수렴한 뒤 영구히 학습 정지 (`softplus`로 교체)
- 기온 손실(σ²≈수십 규모)이 강수 손실(O(1))을 1000배 압도해 강수 헤드가 사실상 학습되지 않음 (분산 정규화)
- baseline을 검증셋이 아닌 전체 데이터에서 계산해 표본 불일치로 인한 허위 "개선"이 발생 (검증셋 동일 표본으로 수정)
- 다중 관측소 확장 시 관측소 경계에서 서울 관측값과 부산의 6시간 후 관측값이 우연히 페어링될 수 있는 경로 존재 (관측소 코드 일치 조건 추가로 원천 차단)
- 테스트 스크립트가 실패 항목을 출력만 하고 종료 코드 0을 반환해, **CI가 어떤 항목이 깨져도 항상 초록불**이었음 (`sys.exit(0 if n_pass == n_all else 1)` 추가). 테스트를 CI에 붙이는 것과 CI가 테스트 결과에 반응하는 것은 별개였습니다.
- 적중률 로그 저장이 배포판에서 `FileNotFoundError`로 죽었음 (2026-08-13 실측). 원자적 저장을 한다며 `tmp = path + ".tmp"`로 **고정된 임시 파일명**을 썼는데, Streamlit Cloud는 세션(브라우저 탭)마다 스레드를 띄우고 5분 자동 갱신마다 이 저장이 호출됩니다. 두 스레드가 같은 tmp에 쓰다가 한쪽이 `os.replace`로 먼저 옮겨가면, 남은 쪽은 방금까지 있던 파일을 잃고 죽습니다. `tempfile.mkstemp()`로 호출마다 고유한 tmp를 쓰도록 고쳤습니다. **`os.replace`가 원자적인 것과 저장 절차 전체가 원자적인 것은 별개였습니다** — 위의 CI 교훈과 같은 형태의 착각입니다.
- 위 크래시를 고친 뒤에도 **조용한 유실 경로가 남아 있었음**. `record_prediction`/`resolve_pending`은 읽기→수정→쓰기 3단계인데 그 사이를 묶는 잠금이 없어, 두 세션이 거의 동시에 읽으면 나중에 쓰는 쪽이 앞선 쪽의 항목을 덮어씁니다. 크래시가 아니라 로그가 소리 없이 사라지는 문제라 증상으로는 드러나지 않습니다 (`threading.Lock`으로 3단계 전체를 묶어 해결, 30스레드×30회 동시 호출에서 900건 전량 보존 확인).
- (2026-08-16) **배치를 32→1024로 키우며 학습률을 √k 배(5.66배) 올렸더니 강수·황사 성능이 실제로 나빠졌습니다.** 대조군(배치만 바꾸고 마스킹은 동일) 없이 봤다면 "13년 확장의 부작용"으로 오진할 뻔했습니다. 배치는 그대로 두고 학습률만 원래 값(1e-3)으로 되돌리자 회복됐습니다 — 배치 크기가 아니라 학습률 스케일링 규칙 자체가 이 모델엔 과했던 것입니다(`train.py` `BATCH_SIZE`/`TRAIN_LR` 환경변수로 분리).
- (2026-08-16) **학습 로그의 최종 요약이 실제로 저장된 체크포인트 값과 달랐습니다.** 요약 출력이 `best_temp_only`(기온이 가장 좋았던 에폭의 값)를 찍었는데, 체크포인트에는 `val_score`(기온+강수 결합 지표)가 가장 좋았던 에폭이 저장됩니다. 두 에폭이 다르면 로그에는 어느 체크포인트도 실제로 낸 적 없는 조합이 찍힙니다(13년 재학습에서 기온 1.3290°C로 찍혔지만 저장된 값은 1.3551°C). 요약이 체크포인트 실값을 찍도록 고쳤고, 둘이 다르면 별도 참고 줄로 알려줍니다.
- (2026-08-16) **적중률 로그가 어느 모델이 만든 예측인지 구분하지 않았습니다.** 체크포인트를 교체하면 이전 모델의 기록과 새 모델의 기록이 같은 로그에 섞여, "누적 적중률"이 어느 모델의 성능도 아닌 혼합값이 됩니다. 예측마다 `model_id`(체크포인트 수정시각+크기)를 태그하고, 화면은 현재 배포된 모델의 기록만 집계하도록 고쳤습니다.
- (2026-08-16) **`backtest_accuracy.py`가 학습에 쓴 표본까지 포함해 적중률을 채우고 있었습니다.** 화면 캡션은 "실제 운영 시각에 실제로 조회 가능했던 데이터로 낸 값"이라고 설명하는데, 백테스트 시딩은 검증/학습 구분 없이 전체 데이터를 모델에 통과시켰습니다 — 오늘 잡은 일 단위 라벨 누수(9번)와 같은 종류의 문제입니다. 체크포인트가 실제로 학습된 분할의 검증 인덱스만 쓰도록 고쳤습니다.
- (2026-08-16) 위 백테스트 시딩이 검증용 27만 건을 그대로 적중률 로그(`cache/accuracy_log.json`)에 합쳐, 파일이 73MB까지 불어나 GitHub 권장 상한(50MB)을 넘었습니다. 6시간마다 자동 커밋되는 파일이라 방치하면 하드 리밋(100MB)에 닿아 조용한 CI 실패를 새로 만들 수 있었습니다. 시딩분을 로컬 전용 파일로 분리하고 실시간 온라인 기록만 남겨 12KB로 되돌렸습니다.
- (2026-08-16) **수치 입력을 12→14차원(연중 시각 sin/cos 추가)으로 바꾸자, 이전 12차원으로 학습된 체크포인트를 로드하는 스크립트들이 정규화 단계(`(vec - mean) / std`)에서 차원이 안 맞아 깨지는 경로가 생겼습니다.** `predict.py`(실제 서빙 경로) 포함 4개 스크립트가 해당됐습니다. 새 축이 벡터 끝에 붙는 구조라, 그 체크포인트가 학습된 차원(`ckpt["num_features"]`)만큼만 앞에서 잘라 쓰도록 방어 로직을 추가해 구버전 체크포인트와도 계속 호환되게 했습니다.
- (2026-08-16) **`head_decouple_finetune.py`로 만든 체크포인트는 `val_temp_mae`/`val_precip_mae`는 갱신하지만 `extreme_metrics`는 갱신하지 않습니다**(원본 체크포인트 값을 그대로 복사). 대시보드 '성능 검증' 탭이 이 필드를 그대로 읽으므로, 패치하지 않으면 실제 배포 모델이 아니라 디커플링 전 성능을 화면에 계속 보여주게 됩니다. 첫 발견 때는 수동으로 패치했는데, 두 번째로 같은 함정에 걸릴 뻔해 `patch_extreme_metrics.py`로 고정했습니다 — 재현되는 실수는 스크립트로 막는 게 다음에도 안전합니다.

</details>

<details id="6-논문의-위상각은-이-구현에서-수학적으로-퇴화합니다">
<summary><b>6. 논문의 '위상각'은 이 구현에서 수학적으로 퇴화합니다</b></summary>

Phase 3-6에서 논문의 위상 필터(θ<30°/30–80°/>80°)를 경보 게이트의 상태로 쓰려고 θ를 실제로 계산해 봤더니, 이 아키텍처에서는 정보가 없는 값이었습니다.

```
θ = atan2(‖w_im·v_im‖, ‖w_re·v_re‖)
  세 인코더가 모두 F.normalize 출력 → ‖v‖ = 1
  ∴ θ = atan2(w_im, w_re)          ← 게이트 배분의 재표현일 뿐
```

`test_phase36.py` T1에서 두 값의 최대 차이가 **1.19e-07**로 확인됩니다. 즉 θ는 축 자체의 표본별 정보를 전혀 담지 못하고, 게이트 가중치를 극좌표로 바꿔 쓴 값입니다. 검색 태스크(논문 원본)에서는 축 임베딩의 노름이 자유로워 θ가 의미를 갖지만, 단위 정규화를 쓰는 이 회귀 구현으로는 그 개념이 이전되지 않습니다. 표본별 축 정보는 축 간 코사인(`cos(Re,Im)` 등, 표준편차 0.109)에만 남아 있어 상태에는 그쪽을 함께 넣었습니다.

</details>

<details id="7-ppo는-이-규모에서-지도학습-기준선을-이기지-못했습니다">
<summary><b>7. PPO는 이 규모에서 지도학습 기준선을 이기지 못했습니다</b></summary>

경보 문제는 RL을 쓸 근거를 의도적으로 갖추도록 설계했습니다 — 비대칭 비용(미탐지 −20 : 오경보 −1), 이산 행동, 그리고 **경보 피로**(최근 경보가 쌓이면 다음 오경보 비용이 커짐)입니다. 피로 항 덕분에 보상이 과거 행동에 의존하므로 이 문제는 밴딧이 아니라 진짜 MDP이고, `test_phase36.py` T5가 이를 수치로 확인합니다(동일 관측·동일 행동인데 이력에 따라 보상 −1.00 vs −2.00).

그런데도 4-fold 교차검증에서 PPO는 졌습니다(−0.173 vs 지도학습 −0.112, 고정 임계값 −0.139). 조건은 대칭으로 맞췄습니다 — 모든 방법이 같은 7개 관측소로 적합하고, 같은 2개 관측소에서 임계값·반복을 고르고, 나머지 3개에서만 평가했습니다.

진 이유는 두 가지로 봅니다.

1. **MDP 구조가 실재하지만 크기가 작습니다.** 최적 경보율이 5~9% 수준인데, 이 구간의 정상상태 피로는 약 0.5라 오경보 비용 배수가 1.5배에 그칩니다. 순차적 결합이 존재는 하되 지배항이 아니라, PPO가 얻을 수 있는 이득 자체가 얇습니다.
2. **표본이 부족합니다.** 이벤트가 전체 214개(적합 겹당 약 130개)입니다. 지도학습은 표본마다 정답 라벨을 직접 받지만 PPO는 같은 신호를 표집된 리턴에서 스스로 찾아내야 합니다. 이 격차가 위 1의 얇은 이득을 덮습니다.

부수적으로 얻은 결과가 하나 더 있습니다. 상태에 게이트 가중치·θ·기압·습도를 넣은 조건(full)은 적합 보상이 −0.132로 가장 좋은데 검증은 −0.200으로 가장 나빴습니다(격차 0.069). 게이트 입력에 위도·경도가 포함돼 있어(`train.record_to_vec` 인덱스 10·11) 이 특성들이 사실상 **관측소 식별자**로 작동했기 때문입니다. 지점 불변 물리량만 남기자 격차가 0.014로 줄었습니다 — 관측망 공백 지대 예측을 표방하는 시스템에서 정책이 지점을 외우고 있었다는 뜻이라, 이 프로젝트 문제의식 자체에 직접 걸리는 결함이었습니다.

</details>

<details id="8-시간-분할-검증이-이-데이터에서는-불가능했습니다">
<summary><b>8. 시간 분할 검증이 이 데이터에서는 불가능했습니다</b></summary>

처음에는 관측소별로 앞 70%를 학습, 뒤 30%를 검증으로 두려 했습니다. 그런데 수집 구간(2026-07-08 ~ 08-07)이 장마 종료 경계를 가로지릅니다.

| 구간 | 평균 강수 | 돌발 강수 이벤트 |
|---|---|---|
| 앞 70% (7/8~7/29, 장마) | 0.695 mm | 212개 / 5,973 (3.55%) |
| 뒤 30% (7/29~8/7, 폭염) | **0.000 mm** | **2개 / 2,573 (0.08%)** |

검증 구간에 비가 아예 오지 않아 평가 자체가 성립하지 않습니다. 관측소를 3개씩 4묶음으로 나눈 교차검증으로 바꿔, 모든 fold가 같은 기간을 공유하게 하고 8,546쌍 전량이 정확히 한 번씩 검증셋이 되도록 했습니다. 30일 수집으로는 계절 일반화를 측정할 수 없다는 뜻이며, 이는 데이터 기간을 1년 이상으로 늘려야 풀리는 문제입니다.

> **후속(2026-08-16)**: 데이터가 13년으로 늘어나며 시간 분할 검증이 처음으로
> 가능해졌습니다. 실제로 돌려본 결과는 아래 9번에 있습니다.

</details>

<details open id="9-검증-분할이-일-단위-라벨을-누수시키고-있었습니다">
<summary><b>9. 검증 분할이 일 단위 라벨을 누수시키고 있었습니다</b> (가장 최근·가장 중요한 발견)</summary>

13년치로 데이터를 확장하며 재학습했더니 강수·극한기상 지표가 전부 나빠졌다
(황사 F1 0.588→0.316). 표본이 4배 늘었는데 성능이 떨어지는 것이 이상해
검증 설계 자체를 의심했다.

**원인**: 폭염·한파·황사 라벨은 (관측소, 날짜) 단위다. 특보가 발효된 날은
그 날 24개 시각 표본이 전부 같은 라벨을 받는다. 그런데 학습·검증 분할은
시각 단위 무작위였다. `split_leakage_check.py`로 실측한 결과, 판정 가능한
검증 표본의 **100.00%**가(양성 표본만 따져도 100.00%) 학습셋에 같은 날짜
짝을 가지고 있었다. 하루 표본이 평균 23.9개라, 한 날이 통째로 한쪽에만
몰릴 확률은 0.8²⁴ ≈ 0.5%에 불과해 사실상 필연적인 결과였다. 모델은 학습
중에 그 관측소·그 날짜의 정답을 이미 본 상태로 검증받고 있었다 — 일반화가
아니라 "같은 날 다른 시각 맞히기"를 채점해 온 셈이다.

날짜 단위로 12개 관측소를 통째로 한쪽에 배정하는 그룹 분할로 바꾸자(관측소
단위가 아니라 날짜 단위로 묶은 이유: Re축이 같은 시각 다른 11개 관측소의
실측값을 입력으로 쓰므로, 관측소별로 쪼개면 입력을 통한 누수가 남는다)
극한기상 F1이 크게 떨어졌다.

| 지표 | 무작위 분할(누수 있음) | 날짜 그룹 분할(누수 없음) |
|---|---|---|
| 폭염 F1 | 0.833 | 0.813 |
| 한파 F1 | 0.658 | 0.384 |
| 황사 F1 | 0.320 | 0.150 |

희소한 사건일수록 낙폭이 컸다(한파 −0.274, 황사 −0.170) — 표본이 적은
사건일수록 "같은 날을 이미 봤다"는 지름길에 더 크게 기대고 있었다는 뜻이다.
폭염(양성률 24%, 기온이라는 강한 물리 신호)은 거의 영향받지 않았다.

**"데이터를 더 쓴 게 손해였나?"를 확인하려고 배포 중이던 이전 체크포인트
(3.6년 학습)와 신규 체크포인트를, 둘 다 학습에 쓰지 않은 동일한 표본
(2013~2022년 중 그룹 분할 검증셋)에서 정면으로 맞대결시켰다.**

| 지표 | 이전 체크포인트(3.6년) | 신규(13년, 그룹 분할) |
|---|---|---|
| 강수 MAE(기준선 대비) | −26.6% | **−18.7%** |
| 폭염 F1 | 0.691 | **0.773** |
| 한파 F1 | **0.392** | 0.374 |
| 황사 F1 | 0.078 | **0.167** |

같은 잣대에서는 신규 모델이 대체로 우세했다 — **이전 체크포인트가 보여주던
높은 F1은 대부분 검증 누수가 만든 착시였다.** 되돌리지 않고 신규 체크포인트를
그대로 배포했다.

이 사건에서 확정한 것: ① 이 프로젝트의 모든 극한기상 F1은 이제부터 날짜
단위 그룹 분할로 측정한다. ② 사건 단위(하루·한 에피소드)로 정의되는 라벨은
그 단위로 분할하지 않으면 검증이 성립하지 않는다는 일반 원칙을 기록해 둔다.

**후속 — 시간 분할 교차검증(2026-08-16).** 그룹 분할과 별개로, 데이터가
13년으로 늘며 처음 가능해진 시간 분할(과거로 학습, 미래로 검증)도 컷오프를
셋으로 바꿔가며 확인했다. 배치 1024·학습률 1e-3·마스킹 ON은 전부 동일.

| 분할 | 검증 구간 | 기온 MAE | 강수(기준선 대비) | 폭염 F1 | 한파 F1 | 황사 F1 |
|---|---|---|---|---|---|---|
| 그룹(날짜 단위) | 2013~2026 전체 중 20% | 1.3551°C | −17.9% | 0.813 | 0.384 | 0.150 |
| 시간, 2021 컷오프 | 2022~2026 | 1.3527°C | −16.6% | 0.774 | 0.408 | 0.106 |
| 시간, 2023 컷오프 | 2024~2026 | 1.3172°C | −18.6% | 0.763 | 0.433 | 0.108 |
| 시간, 2024 컷오프 | 2025~2026 | 1.3311°C | −13.1% | 0.746 | 0.433 | 0.112 |

컷오프를 2021→2023→2024로 늦출수록(검증 구간이 최근에 가까워질수록)
**폭염 F1은 꾸준히 떨어지고(0.774→0.763→0.746) 한파 F1은 꾸준히 오른다
(0.408→0.433→0.433)** — 단일 시간분할 하나만 봤다면 이 방향성을 노이즈로
오판했을 것이다. 연도별 실제 기후·라벨 구성 변화(폭염은 최근일수록 양성률이
높아지는 경향, 한파는 반대)로 해석한다. **황사는 분할 방식과 무관하게
일관되게 낮다(F1 0.11~0.15)** — 표본이나 분할의 문제가 아니라 위에서 뺀
이유(구조적 판별 신호 부족)가 맞다는 추가 증거다.

개발·비교용 기본 잣대는 계속 날짜 그룹 분할을 쓴다(분산이 작고 계절이
양쪽에 고르게 들어간다). 시간 분할은 "실제 운영에서 기대할 성능"을 따로
확인하고 싶을 때 참고 지표로 쓴다.

</details>

## 재학습이 필요한가 — 자동화 검토

**실시간 관측은 모델을 다시 학습시키지 않습니다.** 화면의 모델은 고정된
체크포인트이고, 매 5분 갱신되는 관측값은 두 가지로만 쓰입니다.

1. **추론 입력** — 지금 이 순간의 관측값(+ 이웃 11곳 + 과거 1·3·6h)을 넣어
   +6시간 뒤를 계산한다. **가중치는 건드리지 않는다.**
2. **적중률 기록** — 출력값을 로그에 남기고 6시간 뒤 실측이 확정되면 대조한다.
   **이 대조 결과도 모델을 바꾸지 않는다** — 순수 사후 모니터링이다.

온라인 학습(관측 1건마다 즉시 가중치 갱신)은 **의도적으로 넣지 않았습니다**.

| 근거 | 내용 |
|---|---|
| 표본 밀도 | 관측소당 신규 표본이 **시간당 1건**. 배치 1에폭이 보는 수만 건의 그레디언트 평균과 달리, 1건짜리 업데이트는 노이즈가 학습 신호보다 클 위험이 크다 |
| 재앙적 망각 | 최근 데이터에 맞춰 즉시 갱신하면 계절 패턴을 잊는다. 이를 막을 리플레이 버퍼·정규화 장치가 아직 없다 |
| 재현성 | 배치 재학습은 "이 체크포인트 = 이 데이터 + 이 코드"를 그대로 감사·재현할 수 있다. 온라인 학습은 매 순간 가중치가 달라 같은 상황을 재현하기 어렵다 |

### 그러면 주기적 재학습은 필요한가 — 필요합니다

기후 데이터는 **계절 순환**이 지배적이고, 학습셋에 없는 계절 구간은 외삽이
됩니다. 실제로 이 프로젝트는 초기 30일 수집(2026-07~08, 장마→폭염 경계)에서
**검증 구간에 비가 아예 오지 않아 평가 자체가 성립하지 않는** 문제를 겪었고
(아래 [한계 8](#8-시간-분할-검증이-이-데이터에서는-불가능했습니다)), 데이터를
3.5년으로 늘려서야 해결했습니다. 따라서 재학습 주기는 **분기~반기 단위**가
합리적입니다(월 단위는 신규 표본이 전체의 2% 남짓이라 이득이 얇습니다).

### 자동화 가능 여부 — 부분 자동화가 현실적

| 단계 | 자동화 | 근거 |
|---|---|---|
| 신규 관측 수집 | ✅ **이미 자동** | `.github/workflows/refresh-data.yml` (6h 주기) + `refresh_deploy_data.py` |
| 극한기상 라벨 갱신 | ⚠️ **반자동** | '날씨 이슈별 데이터'·PM10은 포털에서 **수동 다운로드**해야 한다(API 미제공). `import_weather_issues.py`가 파싱은 자동화 |
| 재학습 실행 | ⚠️ **로컬 GPU 필요** | GitHub Actions 무료 러너는 GPU가 없다. 전체 학습 15~35분(GPU 기준) |
| 체크포인트 검증·배포 | ✅ 자동화 가능 | 재학습 후 검증 MAE·F1이 기존보다 나쁘면 배포를 막는 게이트를 CI에 넣을 수 있다 |

**권장 운영 방식**: 관측 수집만 상시 자동으로 돌리고(현재 구현됨), 분기마다
① 포털에서 라벨 CSV 수동 다운로드 → ② 로컬 GPU에서 `train.py` 실행 →
③ 검증 지표가 기존 체크포인트보다 나쁘지 않은지 확인 → ④ 체크포인트 커밋.
③은 이미 규약으로 강제돼 있습니다 — **재학습 전 반드시 백업**하고
(`numerical_trichef_vN_<이유>.pt`), 새 헤드를 추가할 때마다 강수 MAE를
재확인합니다.

## Phase 로드맵

| Phase | 내용 | 상태 |
|---|---|---|
| 1 | ASOS API 연동, 캐싱, fallback | ✅ |
| 2 | 2축 수치 기준 모델 | ✅ |
| 3-1~3 | Z/Re/Im 3축 파이프라인 구축 | ✅ |
| 3-4 | Gram-Schmidt 직교화 ablation, 학습 안정화 | ✅ |
| 3-5 | 입력 조건부 동적 게이팅 | ✅ |
| 3-6 | PPO 경보 게이트(돌발 강수 조기 경보) | ✅ (아래 [한계 6·7](#6-논문의-위상각은-이-구현에서-수학적으로-퇴화합니다) 참조) |
| 4 | Streamlit 대시보드, Docker, CI | ✅ |

## 환경 주의사항

이 저장소의 `requirements.txt`를 전역 Python 환경(venv 미사용)에 설치하면 `numpy<2`, `pillow<11` 등 Streamlit의 의존성 제약 때문에 **같은 환경의 다른 프로젝트가 깨질 수 있습니다.** 가상환경 사용을 강력히 권장합니다.

```bash
python -m venv .venv
.venv\Scripts\activate      # Windows
pip install -r requirements.txt
```

## 부록 — 이전 세대 기록

현재 성능과 직접 비교하지 마십시오(측정 조건·데이터 기간이 다릅니다) — 이력 보존용입니다.

<details>
<summary><b>Tri-CHEF 3축 적용 효과 (2026-08-11, 3.6년 데이터, 무작위 분할)</b></summary>

| 구성 | 기온 MAE | 강수 MAE | 폭염 F1 | 한파 F1 | 황사 F1 |
|---|---|---|---|---|---|
| **Z축 단독** (Re·Im 입력 차단) | 1.4713 °C | 0.2051 mm | 0.752 | 0.389 | 0.206 |
| **3축 Tri-CHEF** (당시 현행) | **1.2554 °C** | **0.1697 mm** | **0.842** | **0.667** | **0.588** |
| 개선폭 | **−14.7%** | **−17.3%** | +0.090 | **+0.278** | **+0.382** |

**공정성 조건**: 같은 데이터 캐시(API 재수집 없음) · 같은 SEED(42, 동일 분할) · 같은 조기 종료 기준(patience 20).
Z축 단독은 아키텍처·용량을 그대로 두고 **Re·Im 입력만 영벡터로 차단**해 정보 기여도만 분리했다(인코더는 동결).
두 체크포인트의 정규화 통계(`mean`/`std`)가 완전히 일치함을 코드로 확인했다.
게이트가 Z축에 99.72%를 배분하고 축 간 코사인이 전부 0.0000인 것이 차단이 실제로 작동했다는 증거다.

**가장 큰 효과는 극한기상 분류에서 나왔다.** 회귀(기온·강수)는 15% 안팎 개선이지만,
황사 F1은 0.206 → 0.588로 **2.9배**, 한파 F1은 0.389 → 0.667로 **1.7배**가 됐다.
드문 사건일수록 "지금 이 지점"만으로는 판단이 어렵고 주변·흐름 정보가 결정적이라는 뜻이다.

> 이 표의 폭염·한파·황사 F1은 무작위 분할(누수 있음)에서 측정한 값입니다.
> Z축 단독 대비 3축의 **상대적** 우위는 두 조건이 같은 조건(같은 누수)에서
> 측정돼 방향성 자체는 유효하나, 절대 F1 수치는 위 그룹 분할 결과를 기준으로
> 삼으십시오.

</details>

<details>
<summary><b>축 구성을 바꿔가며 측정한 이력 (v3~v7, 3.6년 확장 이전 데이터셋·무작위 분할)</b></summary>

이 표 안에서의 상대 비교(v4→v5 등)만 유효하고, 다른 표와 직접 비교하면 안 됩니다
(정규화 통계 불일치 확인됨).

| 단계 | Re축 | Im축 | 기온 MAE | 강수 MAE | 황사 F1 | 게이트 배분 (Re/Im/Z) |
|---|---|---|---|---|---|---|
| v3 | 합성 위성 | MiniLM 텍스트 | 1.450 | 0.2175 | 0.258 | 0.05 / 0.14 / 0.81 |
| v4 | 합성 위성 | MiniLM 텍스트 | 1.387 | 0.2065 | 0.257 | 0.00 / 0.12 / 0.88 |
| v5 | **실측 공간보간** | MiniLM 텍스트 | 1.279 | 0.1696 | **0.661** | 0.52 / 0.04 / 0.44 |
| v7 | 실측 공간보간 | **시간경향벡터** | **1.270** | 0.1735 | 0.576 | 0.37 / 0.21 / 0.42 |

가장 큰 도약은 **Re축을 합성 위성에서 실측 공간보간장으로 교체한 v4→v5** 구간입니다(황사 F1 0.257→0.661). 게이트 배분도 0.00 → 0.52로 바뀌었습니다 — 즉 **모델이 스스로 "이제 이 축은 볼 가치가 있다"고 판단한 것**이며, 이것이 합성 데이터가 정보량 0이었다는 가장 직접적인 증거입니다.

</details>

<details>
<summary><b>Phase 3-6 경보 게이트 (돌발 강수 조기 경보, 관측소 4-fold 교차검증 · 이벤트 214개)</b></summary>

| 정책 | 보상/스텝 | POD | FAR | CSI |
|---|---|---|---|---|
| 항상 정상 | −0.531 | 0.000 | — | 0.000 |
| 고정 임계값 (예측 강수 1개) | −0.139 | 0.603 | 0.656 | **0.280** |
| 지도학습 분류기 (정적 14특성) | **−0.112** | 0.659 | 0.803 | 0.179 |
| PPO (지점 불변 상태) | −0.173 | 0.645 | 0.780 | 0.197 |
| PPO (전체 상태) | −0.200 | 0.607 | 0.835 | 0.149 |

경보 게이트 자체는 크게 유효합니다(−0.531 → −0.112). 다만 **PPO는 두 기준선에 모두 졌습니다** — [한계 7](#7-ppo는-이-규모에서-지도학습-기준선을-이기지-못했습니다) 참조.

</details>

## 참고

원 논문(Tri-CHEF)은 Zenodo에 공개되어 있습니다 — DOI [10.5281/zenodo.20034370](https://doi.org/10.5281/zenodo.20034370), CC BY 4.0.

API 키(`KMA_API_KEY`)는 `.env`에 두며 저장소에 포함되지 않습니다.
