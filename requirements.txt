# Streamlit Community Cloud 배포용 — 대시보드/추론 서빙 전용 최소 의존성.
# 전체 연구 환경(whisper, chromadb, stable-baselines3 등 Phase 3-6 및 향후
# 확장용)은 requirements-research.txt 에 따로 있다. Streamlit Cloud는
# 저장소 루트의 requirements.txt 하나만 보므로, 이 파일이 배포판 기준이다.
#
# torch는 CPU 전용 휠을 명시한다 — 인덱스를 안 밝히면 기본 PyPI가 CUDA
# 빌드를 받으려다 용량 초과·설치 실패로 이어질 수 있다(Dockerfile도 같은
# 이유로 별도 --index-url 로 설치한다).
--extra-index-url https://download.pytorch.org/whl/cpu
torch

streamlit>=1.32.0
streamlit-autorefresh>=1.0.1
plotly>=5.20.0
numpy>=1.26.0,<2
pandas>=2.2.0
requests>=2.31.0
python-dotenv>=1.0.0
sentence-transformers>=3.0.0
transformers>=4.40.0
