name: Refresh deploy data

# Streamlit Community Cloud 의 파일시스템은 휘발성이라, 앱이 실행 중에 쓴
# 데이터는 재시작하면 사라지고 저장소에 커밋된 내용으로 되돌아간다. 그래서
# "앱이 자기 데이터를 유지한다"가 아니라 "저장소가 원본이고 이 워크플로가
# 저장소를 갱신한다"로 구조를 잡는다. 여기서 커밋이 발생하면 Streamlit Cloud
# 가 푸시를 감지해 자동 재배포하고, 그 시점에 새 데이터가 반영된다.
#
# 갱신 대상 2개 (refresh_deploy_data.py 참고):
#   · cache/recent_window.json — 최근 10일 관측 (스파크라인·추이 탭 입력)
#   · cache/accuracy_log.json  — 예보 적중률 로그의 대기 항목 해소
#
# API 호출량: 관측소 12곳 × 경과 시간. 이 값은 **주기와 무관하게 하루 288건**
# 으로 고정된다 — 각 실행이 '마지막 관측 이후의 빈 시간'만 채우는 격차 보충
# 방식이라, 자주 돌리면 회당 건수가 그만큼 줄기 때문이다(2시간 주기면 회당
# 24건 × 12회 = 288건, 6시간 주기면 회당 72건 × 4회 = 288건). 이 키가 차단됐던
# 지점(누적 약 9,800건, CLAUDE.md 4절)의 3% 수준이다.

on:
  schedule:
    # cron 은 UTC 기준이다. 간격이 시간 단위라 KST 로 옮겨도 간격은 같다.
    #
    # 2026-08-19 에 6시간 → 2시간으로 단축했다. 이 창은 두 곳에 쓰인다.
    #   · 추이 탭의 72시간 관측 그래프
    #   · **API 연결 실패 시의 실측 폴백**(weather_collector.set_offline_fallback)
    # 후자가 이번에 추가됐다 — 배포 환경 발신 IP 가 막혔을 때 화면이 날조값
    # 대신 이 창의 실측을 쓴다(README '외부 장애' 절). 그래서 창이 묵으면
    # 폴백 품질이 그대로 나빠진다. 6시간 주기면 최악 6시간 전 관측을 '현재'로
    # 놓고 예측하게 되므로 2시간으로 줄였다.
    #
    # 1시간까지 더 줄이지 않은 이유는 재배포 비용이다. 이 잡이 커밋하면
    # Streamlit Cloud 가 감지해 앱을 재배포하고, 그때마다 캐시가 날아가고
    # 수십 초간 화면이 끊긴다. 1시간 주기는 하루 24회, 2시간은 12회다 —
    # 신선도 3배 개선(6h→2h)을 재배포 3배로 얻는 지점이 균형이라 판단했다.
    # 더 줄이려면 이 줄만 바꾸면 된다(API 예산은 위 주석대로 변하지 않는다).
    - cron: "5 */2 * * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "요청 건수만 계산하고 API 호출·커밋 없이 종료"
        type: boolean
        default: false

# 앞선 실행이 아직 돌고 있으면 겹쳐 돌리지 않는다 — 같은 파일을 두
# 실행이 동시에 쓰면 나중 것이 앞선 수집분을 덮어쓴다.
concurrency:
  group: refresh-deploy-data
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    permissions:
      contents: write        # 갱신 결과를 main 에 되커밋하기 위해 필요

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      # torch·streamlit 은 필요 없다 — 이 잡은 수집과 JSON 가공만 한다.
      - name: Install collector dependencies
        run: pip install requests python-dotenv numpy

      # 키가 없으면 RobustWeatherCollector 가 조용히 폴백으로 내려가
      # "성공 0건"인 채로 워크플로는 초록불이 된다. 그 상태를 실패로 만든다.
      - name: Verify API key is configured
        env:
          KMA_API_KEY: ${{ secrets.KMA_API_KEY }}
        run: |
          if [ -z "$KMA_API_KEY" ]; then
            echo "::error::KMA_API_KEY 시크릿이 없습니다. \
          Settings → Secrets and variables → Actions 에 추가하세요."
            exit 1
          fi

      - name: Refresh window and resolve pending forecasts
        env:
          KMA_API_KEY: ${{ secrets.KMA_API_KEY }}
          # 이 잡은 배치라 관측 미발표 시 기다렸다 다시 받는 게 맞다
          # (기본값 60초 유지). 서빙(app.py)만 0으로 내린다.
          KMA_DAILY_CALL_BUDGET: "1000"
        run: |
          python refresh_deploy_data.py \
            ${{ inputs.dry_run && '--dry-run' || '' }}

      # 신선도 게이트가 실패해도(= 위 스텝 exit 1) 이 스텝은 반드시 돈다.
      #
      # 왜 `!cancelled()` 인가(2026-08-18, 실측으로 원인 규명): 종전 조건은
      # `${{ !inputs.dry_run }}` 뿐이었다. schedule 이벤트에서 inputs 는 비어
      # 있으므로 이 식 자체는 참인데도 스텝이 skipped 됐다 — GitHub Actions 는
      # 상태 함수(success/failure/always/cancelled)가 없는 `if` 에 암묵적으로
      # `success()` 를 AND 로 붙이기 때문이다. 그 결과 게이트가 한 번 실패하면
      # **이미 수집해 디스크에 저장까지 끝낸 관측이 커밋되지 않고 러너와 함께
      # 폐기**됐다. 저장소는 낡은 창 그대로 남고, 다음 실행도 같은 낡은 기준점
      # 에서 다시 시작한다. 실제로 run #30·#31 이 연속 실패하며 배포 화면에
      # 17시간 묵은 데이터가 노출됐다(수집 자체는 정상이었고, 같은 시각 API 를
      # 직접 호출하면 04:00~17:00 이 전부 SUCCESS_LIVE 로 응답했다).
      #
      # 즉 게이트는 제 역할(장애 감지)을 했으나, 실패 처리 방식이 부분 진척을
      # 통째로 버려 일시적 장애를 장기 정지로 키웠다. 관측소 하나만 계속
      # 뒤처져도 나머지 11곳의 갱신이 영구히 막히는 구조이기도 했다.
      #
      # 그래서 **저장과 경보를 분리한다** — 수집분은 상태와 무관하게 커밋하고
      # (창은 항상 갈 수 있는 데까지 전진), 묵음 여부는 아래 별도 스텝이
      # 잡 전체를 빨간불로 만들어 알린다. `always()` 대신 `!cancelled()` 를
      # 쓰는 이유는 수동 취소 시까지 커밋하지 않기 위함이다.
      - name: Commit refreshed data (게이트 실패와 무관하게 수집분 보존)
        if: ${{ !cancelled() && !inputs.dry_run }}
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add cache/recent_window.json cache/accuracy_log.json cache/api_usage_daily.json
          if git diff --cached --quiet; then
            echo "변경 없음 — 커밋 생략 (이미 최신이거나 신규 관측 0건)"
            exit 0
          fi
          git commit -m "데이터 갱신: 최근 관측 창 + 적중률 로그 대조 (자동)"
          # 커밋과 푸시 사이에 사람이 main 에 푸시하면 non-fast-forward 로
          # 거부되므로 rebase 로 다시 얹는다.
          #
          # rebase --abort 가 반드시 먼저 와야 한다(2026-08-19 수정). 이 잡이
          # 건드리는 cache/*.json 은 파일 전체가 한 줄로 재생성되므로, 원격이
          # 같은 파일을 건드렸으면 충돌은 가능성이 아니라 **필연**이다. 그때
          # 러너는 rebase 진행 중 상태로 남고, 뒤이은 재시도는 네트워크에
          # 닿지도 못한 채 "Pulling is not possible... unmerged files" 로
          # 죽으면서 화면에는 엉뚱하게 "원격이 앞서 있음"만 찍힌다.
          #
          # 충돌 시 러너가 방금 수집한 쪽을 택한다 — 이 파일들은 관측을 다시
          # 받아 통째로 재생성한 결과라 우리 것이 더 최신이다.
          #
          # 여기서 `-X theirs` 인 것이 맞다. rebase 는 upstream 을 체크아웃한 뒤
          # 내 커밋을 그 위에 replay 하므로 ours/theirs 가 뒤집힌다 —
          # ours=원격(upstream), theirs=내 커밋. 실측(2026-08-19)으로 확인:
          #   -X ours   → 결과 REMOTE      (수집분이 버려진다. 틀림)
          #   -X theirs → 결과 LOCAL-fresh (수집분이 남는다. 맞음)
          for i in 1 2 3; do
            git rebase --abort 2>/dev/null || true
            git pull --rebase -X theirs --autostash origin main && git push && exit 0
            echo "push 재시도 $i/3 (원격이 앞서 있거나 rebase 충돌)"
            sleep 5
          done
          git rebase --abort 2>/dev/null || true
          echo "::error::3회 시도 후에도 push 실패"
          exit 1
