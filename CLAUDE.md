# CLAUDE.md — 작업 규약

Tri-CHEF 기후 예보 프로젝트. 이 문서는 **반드시 선행되어야 할 규약**만 담는다.
프로젝트 설명은 README.md, 성능 수치는 학습 로그가 원본이다.

---

## 1. 절대 규칙

1. **측정하지 않은 것을 주장하지 않는다.** "개선될 것"이 아니라 "실측 결과 X→Y".
   추측이면 추측이라고 명시한다.
2. **실패를 숨기지 않는다.** baseline보다 나쁘면 나쁘다고 쓴다. 지금도 강수 MAE는
   "상시 0 예측"보다 나쁘다(-19.3%). 이 사실을 가리는 표현을 쓰지 않는다.
3. **재학습 전에 백업한다.** `checkpoints/numerical_trichef.pt`는 학습이 덮어쓴다.
   `cp checkpoints/numerical_trichef.pt checkpoints/numerical_trichef_vN_<이유>.pt`
4. **하드코딩 금지 — 데이터에서 산출한다.** prior·pos_weight·워밍업 에폭은 전부
   학습 표본에서 계산한다. 임계값처럼 규정으로 정해진 값만 상수로 둔다.
5. **가짜 데이터를 입력으로 쓰지 않는다.** Re축이 정보량 0이었던 원인.

## 2. 검증 규약

- **임계값은 반드시 보정용/평가용을 분리해서 고른다.** 같은 데이터에서 고르고
  채점하면 허수가 섞인다(2026-08-08 실측 확인). `hurdle_diagnose.py` 참조.
- **baseline은 검증셋과 동일 표본에서 계산한다.** 전체 평균과 비교하면 표본
  차이만으로 "개선"처럼 보인다.
- **양성이 희박한 이벤트는 accuracy를 쓰지 않는다.** precision/recall/F1만 본다.
  폭염 1.4%, 한파 0.17% 수준이라 "항상 음성"이 accuracy 99%를 넘는다.
- **표본 부족은 튜닝으로 못 고친다.** 강풍(≈300개)·우박(≈9개)은 제외 확정.
  F1 상한은 표본 수가 좌우한다(폭염 8,745개→0.83, 황사 637개→0.26).

## 3. 실행 방법

학습·진단은 전부 GPU 컨테이너에서 돈다. 로컬에 torch가 없다.

```bash
sg docker -c "docker run --rm --gpus all -v \$(pwd):/app tri-chef-train-gpu:latest <스크립트>.py"
```

- 컨테이너 stdout은 **종료 시점에 한꺼번에 flush**된다. 진행률이 안 보이는 건
  정상이다. 백그라운드로 돌리고 완료 알림을 기다린다.
- 전체 학습은 보통 15~35분, 시스템 부하에 따라 1시간까지 간다.
- `docker` 명령은 `sg docker -c "..."`로 감싼다(그룹 권한).

## 4. 데이터 규약

- **수집은 웹 포털 CSV**로 한다. apihub API는 약 9,800건에서 차단당한 이력이
  있다. 대량 수집을 API로 재시도하지 않는다.
- **원본 CSV는 EUC-KR**이다. UTF-8로 읽거나 grep하면 조용히 실패한다.
- **결측을 기본값으로 채우지 않는다.** 채우면 결측이 실측으로 위장된다
  (`_sf_or_none` 패턴 — 하나라도 결측이면 레코드 전체를 스킵).
- **학습 캐시(`cache/text_embeddings.npz` 439MB, `historical_data_1y.json` 64MB)를
  배포 산출물에 넣지 않는다.** 배포는 `build_recent_window.py`가 만든
  `recent_window.json`(0.5MB)만 읽는다.

## 5. 아키텍처 — 확정 사항

| 축 | 내용 | 근거 |
|---|---|---|
| Z | 국소 점 상태(수치 12차원) | 게이트 배분 ~87%, 실질적 주력 |
| Re | 공간 IDW 보간장(이웃 11개 관측소) | 가짜 위성은 정보량 0으로 실측됨 |
| Im | 시간 경향 벡터(1h/3h/6h 전 대비 변화율, 12차원) | 실측이나 MiniLM 대비 F1 하락, 원칙 우선으로 채택 |

- **Tri-CHEF 유지 확정.** 제거하면 강수가 5.5% 더 나빠진다(2026-08-09 재검증).
- **위성 인코더(torchvision)는 서빙에서 제외.** Re축 원본은 기여도 정확히 0이었다.
- **헤드가 늘면 강수가 나빠진다**(공유 magnitude 경쟁). `EXTREME_BCE_WEIGHT=0.3`으로
  완화 중. 새 헤드를 추가할 때마다 강수 MAE를 반드시 재확인한다.
- **대설은 독립 헤드로 만들지 않는다.** 한파∧호우의 논리곱이라 중복계산이 된다.
- **Im축은 성능보다 원칙(가짜 데이터 금지)을 우선해 확정.** 시간 경향 벡터(실측)로
  교체 후 인코더 용량(32→128)을 키워도 MiniLM 시뮬레이션 텍스트보다 4개 F1
  지표 전부 낮다(2026-08-10 실측: 강수 0.585→0.571, 폭염 0.880→0.862, 한파
  0.765→0.730, 황사 0.671→0.602). "용량 부족" 가설은 기각됐다 — 원인은 여전히
  불명확하나(정보량 자체가 약하거나, 결측 시 중립값 대체가 노이즈를 더할 수
  있음), MiniLM은 실측 Z축의 재포장이라 완전한 가짜(Re축 위성)와는 성격이
  다르므로 "가짜 유지"보다 "실측 유지 + 성능 손해 명시"를 택했다.

## 6. 스코프 밖

- **법적 제약**: 기상산업진흥법상 예보업은 등록 대상(기상예보사 필요). 불특정
  다수 대상 서비스로 배포하지 않는다. "예보·특보·경보" 대신 "모델 출력값"으로
  표기하고 면책 고지를 단다.
- **적용 불가 논문**: GraphCast·Pangu·FourCastNet·ClimaX는 ERA5 격자 전제라
  12개 지점 데이터에 이전되지 않는다. NowcastNet의 이류-확산 손실도 우리
  보간장에 걸면 자기 인공물을 학습하게 된다.
- **가뭄·기후변화감시**: 수주~수개월 누적 지표라 +6시간 예보 프레임과 안 맞는다.

## 7. 파일 정리

- 더 이상 쓰지 않는 파일은 삭제하지 말고 `삭제예정/`으로 옮긴다.
- `collect_year.py`는 수집 경로가 폐기됐어도 유틸리티(`load_existing`,
  `atomic_save`)로 import되니 유지한다.
- `rl_phase_filter.py`·`test_phase36.py`는 CI가 실행하므로 유지한다.
